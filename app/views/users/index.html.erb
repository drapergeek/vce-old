<h2>Users</h2>
<table class="pretty">
  <tr>
    <th>E-mail</th>
    <th>First</th>
    <th>Last</th>
  </tr>  
  <% @users.each do |u| %>
    <tr>
      <td><%= u.email %></td>
      <td><%= u.fname %></td>
      <td><%= u.lname %></td>
      <td><%= link_to "Show", u %></td>
      <td><%= link_to "Edit", edit_user_path(u) %></td>
      <td><%= button_to "destroy", u,:method=>:delete, :confirm=>"Are you sure?" %></td>
    </tr>
  <% end %>
</table>
<% if can? :create, User %>
<%= link_to "New User", new_user_path %>
<% end %>
