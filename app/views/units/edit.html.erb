<h1>Editing unit</h1>

<% form_tag  :action=>'update', :id => @unit do%>

  <%= render :partial => 'form' %>
  <%= submit_tag 'Update' %>

<% end %>

<%= link_to 'Show', @unit %> |
<%= link_to 'Back', :controller=>'units' %>
